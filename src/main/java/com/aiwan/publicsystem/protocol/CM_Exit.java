package com.aiwan.publicsystem.protocol;

import com.aiwan.publicsystem.annotation.ProtocolAnnotation;
import com.aiwan.util.Protocol;

@ProtocolAnnotation(protocol = Protocol.EXIT)
public class CM_Exit {
}
